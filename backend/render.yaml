services:
  - type: web
    name: recordbook-backend
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: MONGO_URI
        value: <your-mongodb-uri>
      - key: PORT
        value: 8000
